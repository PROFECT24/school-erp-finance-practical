# AI Prompts Used

These are Claude/ChatGPT-compatible prompts used as the development plan for the practical.

1. Read the Laravel practical PDF and identify the required modules, fields, formulas, report filters, deliverables, and evaluation priorities.
2. Convert the supplied static school finance admin template into Laravel Blade pages without redesigning the UI.
3. Generate Laravel migrations/models/controllers for Income, Expense, and Payroll CRUD with validation and JSON API support.
4. Implement dashboard totals using `Income - (Expenses + Payroll)` and make dashboard cards dynamic.
5. Create professional DomPDF views for fee receipt PDF and financial summary PDF with print-ready tables.
6. Add Excel-compatible financial report export from the same report data source used by the PDF and dashboard.
7. Prepare README, SQL seed file, Postman collection, and tests for evaluator handoff.
8. Refactor API authentication to Laravel Sanctum Bearer tokens and update Postman documentation.
